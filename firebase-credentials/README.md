# Put Firebase service account JSON here as:
#   service-account.json
#
# Download: Firebase Console → Project settings → Service accounts →
#   Generate new private key
#
# Never commit the real JSON file.
