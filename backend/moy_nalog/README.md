"""
Мой налог (НПД) — авточеки для самозанятых мастеров.

## Зачем

При онлайн-оплате услуги/заказа Вместе может автоматически зарегистрировать
доход в приложении «Мой налог» (чек НПД). ЮKassa и другие эквайеры для
самозанятых часто не фискализируют платёж — обязанность остаётся на мастере.

## Как работает

1. Организация в кабинете подключает «Мой налог» (ИНН+пароль ЛК ФЛ или SMS).
2. Refresh-токен и device id хранятся **зашифрованными** (Fernet от SECRET_KEY).
3. При `mark_booking_paid` / оплате кафе — одна идемпотентная попытка выбить чек
   (`NpdReceipt`, уникальность source+source_id).
4. Исходы: issued / failed (можно retry) / maybe (таймаут после отправки —
   автоповтор запрещён, нужна ручная проверка в приложении ФНС).

## Важно

- API `lknpd.nalog.ru` **неофициальный** (как у мобильного приложения), контракт
  может измениться. Долгосрочно — партнёрский API ФНС для платформ.
- Токен = доступ к налоговому кабинету мастера. Не логируем секреты.
- Сервер должен достучаться до `lknpd.nalog.ru` (РФ-хостинг предпочтителен).

## API

- `GET/PATCH /api/moy-nalog/status/`
- `POST /api/moy-nalog/connect/password/`
- `POST /api/moy-nalog/connect/sms/start|verify/`
- `POST /api/moy-nalog/disconnect/`
- `GET /api/moy-nalog/receipts/`
- `POST /api/moy-nalog/receipts/<id>/retry/`
"""
